import { Card, CardBody, CardTitle, CardSubTitle, Button } from 'reactstrap'
import { NavLink } from 'react-router-dom'

const AnimeIndex = ({anime}) => {
    return (
        <>
            <h2>a bunch of anime</h2>
        </>
  )
}

export default AnimeIndex